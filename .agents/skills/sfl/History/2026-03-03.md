# SFL History - 2026-03-03

## 20:23 - Added pr-approve command

Added `pr-approve` to SKILL frontmatter/scripts reference and created `scripts/pr-approve.ps1` to approve a PR by number with auth preflight.

## 20:27 - Added auto-select and criteria gate

Updated `scripts/pr-approve.ps1` so PR number is optional; it now selects the oldest non-draft `human:ready-for-review` PR and only approves when SFL readiness criteria are satisfied.

## 21:06 - Captured approval identity lesson

Recorded in `docs/lessons.md` that self-approval is blocked, alternate reviewer accounts must have repo access, and admin merge is fallback when no valid reviewer identity exists.
