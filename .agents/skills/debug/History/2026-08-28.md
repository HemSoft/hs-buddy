# Debug history, 2026-08-28

## 01:03 - Verified the Issue #606 workbench correction

Reproduced the released Active repositories section collapsing to a title-height strip over CDP, traced it to flex shrink inside the overview scroller, and validated the fix in the actual isolated Electron branch at 2048x984 and 1440x900. The corrected workbench matched its content height, used vertical page scrolling without horizontal overflow, and kept the featured repository plus issue and pull-request lanes readable. Retrospective found no reusable debug change because the existing real-Electron CDP workflow exposed the renderer-only layout defect directly.

## 04:16 - Verified the Issue #608 equal-width layout

Measured the old half-width cards and every corrected card in the actual isolated Electron app at desktop and narrow sizes, confirming equal full widths, comfortable side-by-side lanes on desktop, stacked lanes on narrow windows, and no horizontal overflow. Retrospective updated the debug skill with an Aspire CLI version-alignment fallback after the global 13.6 CLI failed against the repository's pinned 13.5 SDK.
