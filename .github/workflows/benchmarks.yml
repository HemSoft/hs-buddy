name: Benchmarks

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read

concurrency:
  group: benchmarks-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  benchmarks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          fetch-depth: 2

      - name: Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: 24

      - name: Install Bun
        run: npm install -g bun@1.3.7

      - name: Cache Bun dependencies
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: ${{ runner.os }}-bun-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Determine comparison baseline
        id: baseline
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            baseline_ref="${{ github.event.pull_request.base.sha }}"
          elif [[ "${{ github.event_name }}" == "push" && "${{ github.event.before }}" != "0000000000000000000000000000000000000000" ]]; then
            baseline_ref="${{ github.event.before }}"
          else
            baseline_ref="$(git rev-parse HEAD^)"
          fi
          echo "ref=${baseline_ref}" >> "$GITHUB_OUTPUT"

      - name: Checkout comparison baseline
        shell: bash
        run: |
          git fetch --no-tags --depth=1 origin "${{ steps.baseline.outputs.ref }}"
          git worktree add --detach "$RUNNER_TEMP/bench-baseline" FETCH_HEAD

      - name: Run baseline benchmarks on this runner
        shell: bash
        run: |
          cd "$RUNNER_TEMP/bench-baseline"
          bun install --frozen-lockfile
          bun run bench:json
          cp bench-results.json "$GITHUB_WORKSPACE/bench-baseline.json"

      - name: Run benchmarks (JSON output)
        run: bun run bench:json

      - name: Compare against baseline
        run: bun run bench:compare

      - name: Upload benchmark results
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: bench-results
          path: bench-results.json
          if-no-files-found: warn
          retention-days: 30
