# SFL Dispatcher — lightweight gate that prevents wasteful gh-aw runs
#
# Runs every 30 minutes and checks GitHub state with simple gh CLI calls.
# Only dispatches gh-aw workflows when there's actual work to do:
#   - Issue Processor: when claimable agent:fixable issues exist and PR creation is allowed
#   - PR Analyzers A/B/C: when draft PRs with agent:pr exist
#   - PR Fixer: when draft PRs with agent:pr AND all 3 analyzer markers exist
#   - PR Promoter: when draft PRs are ready for promotion OR approved PRs await merge
#
# This eliminates ~240 no-op Copilot inference calls/day.
#
# DEPLOYMENT: Copy this file to the consumer repo's .github/workflows/ directory.
# Adjust workflow filenames in the Dispatch steps if your consumer uses different names.

name: "SFL Dispatcher"

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

permissions:
  contents: read
  issues: read
  pull-requests: read
  actions: write

concurrency:
  group: "sfl-dispatcher"

jobs:
  dispatch:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.GH_AW_GITHUB_TOKEN || github.token }}
      REPO: ${{ github.repository }}
    steps:
      - name: Check pull request creation permission
        id: pr-permission
        run: |
          set -euo pipefail

          CAN_CREATE_PRS=false
          REASON="Unable to verify repository workflow permissions."

          if PERMISSIONS=$(gh api "repos/$REPO/actions/permissions/workflow" \
            --jq '[.default_workflow_permissions, (.can_approve_pull_request_reviews | tostring)] | @tsv' \
            2>/tmp/pr_permission_error); then
            DEFAULT_PERMISSION=$(echo "$PERMISSIONS" | cut -f1)
            CAN_APPROVE=$(echo "$PERMISSIONS" | cut -f2)

            if [ "$DEFAULT_PERMISSION" = "write" ] && [ "$CAN_APPROVE" = "true" ]; then
              CAN_CREATE_PRS=true
              REASON="Repository Actions workflow permissions allow PR creation."
            else
              REASON="Repository Actions workflow permissions block PR creation: default=$DEFAULT_PERMISSION allow_prs=$CAN_APPROVE."
            fi
          else
            REASON="Unable to verify repository workflow permissions. See the preflight log."
            tr '\n' ' ' < /tmp/pr_permission_error | head -c 240 || true
            echo ""
          fi

          echo "can_create_prs=$CAN_CREATE_PRS" >> "$GITHUB_OUTPUT"
          echo "reason=$REASON" >> "$GITHUB_OUTPUT"
          echo "$REASON"

      - name: Check for PR fallback issues
        id: pr-fallbacks
        run: |
          set -euo pipefail

          COUNT=$(gh issue list --repo "$REPO" --state open --label "agent:pr" \
            --json number,body --limit 100 \
            --jq '[.[] | select((.body // "") | contains("This was originally intended as a pull request"))] | length')
          echo "pr_fallback_count=$COUNT" >> "$GITHUB_OUTPUT"

          if [ "$COUNT" -gt 0 ]; then
            echo "Found $COUNT create_pull_request fallback issue(s) — blocking Issue Processor dispatch."
          else
            echo "No create_pull_request fallback issues found."
          fi

      - name: Check for claimable fixable issues
        id: issues
        run: |
          set -euo pipefail

          COUNT=$(gh issue list --repo "$REPO" --label "agent:fixable" --label "action-item" \
            --state open --json number,labels --limit 100 \
            --jq '[.[] | select([.labels[].name] | (
              index("agent:in-progress") == null and
              index("agent:pause") == null and
              index("agent:human-required") == null and
              index("agent:blocked") == null and
              index("no-agent") == null
            ))] | length')
          echo "fixable_count=$COUNT" >> "$GITHUB_OUTPUT"

          if [ "$COUNT" -gt 0 ]; then
            echo "Found $COUNT claimable fixable issue(s)."
          else
            echo "No claimable fixable issues — skipping Issue Processor."
          fi

      - name: Check for draft PRs with agent:pr
        id: prs
        run: |
          set -euo pipefail

          PRS=$(gh pr list --repo "$REPO" --state open --draft --label "agent:pr" \
            --json number,body,labels,createdAt \
            --jq '[.[] | select(.labels | map(.name) | index("agent:human-required") | not)]')

          COUNT=$(echo "$PRS" | jq 'length')
          echo "draft_pr_count=$COUNT" >> "$GITHUB_OUTPUT"

          if [ "$COUNT" -eq 0 ]; then
            echo "No eligible draft PRs — skipping analyzers and fixer."
            echo "fixer_ready=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "Found $COUNT eligible draft PR(s) — will dispatch analyzers."

          OLDEST=$(echo "$PRS" | jq -r 'sort_by(.createdAt) | .[0]')
          PR_NUMBER=$(echo "$OLDEST" | jq '.number')
          PR_BODY=$(echo "$OLDEST" | jq -r '.body // ""')
          PR_LABELS=$(echo "$OLDEST" | jq -c '[.labels[].name]')

          CYCLE=0
          for N in 1 2 3; do
            if echo "$PR_LABELS" | jq -e "index(\"pr:cycle-$N\")" > /dev/null 2>&1; then
              CYCLE=$N
            fi
          done

          ALL_MARKERS=true
          for ANALYZER in a b c; do
            if ! echo "$PR_BODY" | grep -qF "[MARKER:pr-analyzer-$ANALYZER cycle:$CYCLE]"; then
              ALL_MARKERS=false
              break
            fi
          done

          if echo "$PR_BODY" | grep -qF "[MARKER:pr-fixer cycle:$CYCLE]"; then
            echo "PR #$PR_NUMBER already fixed in cycle $CYCLE — fixer not needed."
            echo "fixer_ready=false" >> "$GITHUB_OUTPUT"
          elif [ "$ALL_MARKERS" = "true" ]; then
            echo "PR #$PR_NUMBER has all 3 analyzer markers for cycle $CYCLE — will dispatch fixer."
            echo "fixer_ready=true" >> "$GITHUB_OUTPUT"
          else
            echo "PR #$PR_NUMBER missing analyzer markers for cycle $CYCLE — fixer not ready."
            echo "fixer_ready=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Dispatch Issue Processor
        if: steps.issues.outputs.fixable_count != '0' && steps.pr-permission.outputs.can_create_prs == 'true' && steps.pr-fallbacks.outputs.pr_fallback_count == '0'
        run: |
          echo "Dispatching Issue Processor..."
          gh workflow run issue-processor.lock.yml --repo "$REPO"

      - name: Dispatch PR Analyzers
        if: steps.prs.outputs.draft_pr_count != '0'
        run: |
          echo "Dispatching PR Analyzer A..."
          gh workflow run pr-analyzer-a.lock.yml --repo "$REPO"
          echo "Dispatching PR Analyzer B..."
          gh workflow run pr-analyzer-b.lock.yml --repo "$REPO"
          echo "Dispatching PR Analyzer C..."
          gh workflow run pr-analyzer-c.lock.yml --repo "$REPO"

      - name: Dispatch PR Fixer
        if: steps.prs.outputs.fixer_ready == 'true'
        run: |
          echo "Dispatching PR Fixer..."
          gh workflow run pr-fixer.lock.yml --repo "$REPO"

      - name: Check for approved PRs awaiting merge
        id: approved
        run: |
          set -euo pipefail

          PRS=$(gh pr list --repo "$REPO" --state open --label "human:ready-for-review" \
            --json number,isDraft,reviews \
            --jq '[.[] | select(.isDraft == false)]')

          APPROVED=0
          for ROW in $(echo "$PRS" | jq -c '.[]'); do
            HAS_APPROVAL=$(echo "$ROW" | jq '[.reviews | group_by(.author.login) | .[] | last | select(.state == "APPROVED")] | length')
            if [ "$HAS_APPROVAL" -gt 0 ]; then
              APPROVED=$((APPROVED + 1))
            fi
          done

          echo "approved_count=$APPROVED" >> "$GITHUB_OUTPUT"

          if [ "$APPROVED" -gt 0 ]; then
            echo "Found $APPROVED approved PR(s) awaiting merge — will dispatch PR Promoter."
          else
            echo "No approved PRs awaiting merge."
          fi

      - name: Dispatch PR Promoter
        if: steps.prs.outputs.draft_pr_count != '0' || steps.approved.outputs.approved_count != '0'
        run: |
          echo "Dispatching PR Promoter..."
          gh workflow run pr-promoter.lock.yml --repo "$REPO"

      - name: Summary
        run: |
          # shellcheck disable=SC2129
          echo "## SFL Dispatcher Summary" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "| Workflow | Dispatched |" >> "$GITHUB_STEP_SUMMARY"
          echo "|----------|------------|" >> "$GITHUB_STEP_SUMMARY"

          if [ "${{ steps.issues.outputs.fixable_count }}" = "0" ]; then
            echo "| Issue Processor | No (0 claimable fixable issues) |" >> "$GITHUB_STEP_SUMMARY"
          elif [ "${{ steps.pr-permission.outputs.can_create_prs }}" != "true" ]; then
            echo "| Issue Processor | No (${{ steps.pr-permission.outputs.reason }}) |" >> "$GITHUB_STEP_SUMMARY"
          elif [ "${{ steps.pr-fallbacks.outputs.pr_fallback_count }}" != "0" ]; then
            echo "| Issue Processor | No (${{ steps.pr-fallbacks.outputs.pr_fallback_count }} PR fallback issues need audit repair) |" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "| Issue Processor | Yes (${{ steps.issues.outputs.fixable_count }} claimable fixable issues) |" >> "$GITHUB_STEP_SUMMARY"
          fi

          if [ "${{ steps.prs.outputs.draft_pr_count }}" != "0" ]; then
            echo "| PR Analyzer A/B/C | Yes (${{ steps.prs.outputs.draft_pr_count }} draft PRs) |" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "| PR Analyzer A/B/C | No (0 draft PRs) |" >> "$GITHUB_STEP_SUMMARY"
          fi

          if [ "${{ steps.prs.outputs.fixer_ready }}" = "true" ]; then
            echo "| PR Fixer | Yes (markers complete) |" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "| PR Fixer | No (markers incomplete) |" >> "$GITHUB_STEP_SUMMARY"
          fi

          PROMOTER_DISPATCHED="No"
          PROMOTER_REASON=""
          if [ "${{ steps.prs.outputs.draft_pr_count }}" != "0" ]; then
            PROMOTER_DISPATCHED="Yes"
            PROMOTER_REASON="${{ steps.prs.outputs.draft_pr_count }} draft PRs"
          fi
          if [ "${{ steps.approved.outputs.approved_count }}" != "0" ]; then
            PROMOTER_DISPATCHED="Yes"
            if [ -n "$PROMOTER_REASON" ]; then
              PROMOTER_REASON="$PROMOTER_REASON + ${{ steps.approved.outputs.approved_count }} approved PRs"
            else
              PROMOTER_REASON="${{ steps.approved.outputs.approved_count }} approved PRs"
            fi
          fi
          if [ "$PROMOTER_DISPATCHED" = "Yes" ]; then
            echo "| PR Promoter | Yes ($PROMOTER_REASON) |" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "| PR Promoter | No (no draft or approved PRs) |" >> "$GITHUB_STEP_SUMMARY"
          fi
