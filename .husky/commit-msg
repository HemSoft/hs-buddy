set -e

# Validate commit message follows conventional commits
bunx commitlint --edit "$1"

# Populate CHANGELOG.md from conventional commit message
bun scripts/changelog-from-commit.ts "$1"
git add CHANGELOG.md
