# SFL Workflow Sequence — Annotated Architecture

> Companion document for `sfl-architecture-flow-v2.png`.
> Documents every workflow, label, and artifact in execution order.

---

## Table of Contents

1. [Entry Points](#1-entry-points)
2. [Labels](#2-labels)
3. [Workflows](#3-workflows)
4. [Sequence — Step by Step](#4-sequence--step-by-step)
5. [State Machine Summary](#5-state-machine-summary)

---

## 1. Entry Points

There are three ways work enters the SFL pipeline:

| # | Entry Point | How It Works |
|---|-------------|-------------|
| A | **Scheduled Workflows** | `repo-audit` and `simplisticate` run daily, discover findings, and produce discussion reports |
| B | **Discussion Label Trigger** | When a discussion is labeled `action-item`, the `SFL Discussion Processor` (`discussion-processor`) fires and creates issues |
| C | **Chat Session (Human)** | A human asks Copilot to "create an SFL issue that fixes…" — the issue is created with `agent:fixable` + `action-item` labels |

---

## 2. Labels

### Lifecycle Labels (Issue)

| Label | Color | Description | Applied By | Consumed By |
|-------|-------|-------------|-----------|-------------|
| `action-item` | 🔵 `#1f6feb` | Actionable item — automation will process and generate a PR | repo-audit, simplisticate, discussion-processor | discussion-processor |
| `agent:fixable` | 🟢 `#2ea44f` | Agent has analyzed this and can auto-fix safely | discussion-processor, human | sfl-issue-processor (via dispatcher) |
| `agent:in-progress` | 🔵 `#0075ca` | Agent is actively working on this item | sfl-issue-processor | sfl-auditor |
| `agent:human-required` | 🔴 `#d73a4a` | Fix exceeds safe automation boundary — human must own | sfl-issue-processor, sfl-auditor | human |
| `agent:escalated` | 🔴 `#b60205` | Escalated after repeated failures; senior review needed | sfl-auditor | human |
| `agent:pause` | 🟠 `#e3771a` | Agent halted; issue needs human intervention before retry | sfl-auditor | human |

### Lifecycle Labels (PR)

| Label | Color | Description | Applied By | Consumed By |
|-------|-------|-------------|-----------|-------------|
| `agent:pr` | ⬜ `#ededed` | Marks a PR as SFL-managed | sfl-issue-processor | analyzers, fixer, promoter, dispatcher |
| `agent:promoted` | 🔵 `#1D76DB` | PR promoted from draft to ready-for-review | pr-promoter | human (informational) |
| `pr:cycle-1` … `pr:cycle-3` | 🔵→⬜ gradient | Tracks how many analyze→fix cycles the PR has been through | pr-fixer | dispatcher, promoter |
| `human:ready-for-review` | 🟣 `#6f42c1` | PR is ready for human review and merge decision | pr-promoter | pr-label-actions, human |

### Risk Labels

| Label | Color | Description |
|-------|-------|-------------|
| `risk:trivial` | ⬜ `#cfd3d7` | Zero-risk: formatting, typos, doc updates |
| `risk:low` | 🟢 `#0e8a16` | Low-risk: dependency bumps, safe refactors |
| `risk:medium` | 🟡 `#fbca04` | Medium-risk: logic changes, new features |
| `risk:high` | 🔴 `#e11d48` | High-risk: auth, payments, data migrations |

### Classification Labels

| Label | Color | Description |
|-------|-------|-------------|
| `report` | ⬜ `#8b949e` | Informational output only — automation must not act on this |
| `audit` | ⬜ `#ededed` | Produced by audit workflows |
| `source:repo-audit` | 🔵 `#bfd4f2` | Generated by repo-audit |

---

## 3. Workflows

### 3.1 Scheduled Discovery Workflows

These run on a cron schedule and produce **findings** as Discussions or Issues.

#### `repo-audit` — Daily Repository Audit

| Field | Value |
|-------|-------|
| **Type** | Agentic (`.md` + `.lock.yml`) |
| **Schedule** | Daily |
| **Trigger** | `schedule: daily`, `workflow_dispatch` |
| **Model** | (platform default) |
| **Permissions** | `contents: read`, `issues: read`, `pull-requests: read`, `discussions: read` |
| **Safe-Outputs** | `create-discussion` (max 1, prefix `[repo-audit]`), `update-discussion` (max 5), `add-comment` (max 1) |
| **Input** | Full repository state |
| **Output** | Single consolidated Discussion with all findings |
| **Artifact State** | `010-issue-created` (when Discussion is labeled `action-item`) |

#### `simplisticate` — Daily Simplification Audit

| Field | Value |
|-------|-------|
| **Type** | Agentic (`.md` + `.lock.yml`) |
| **Schedule** | `0 6 * * *` (1:00 AM EST) |
| **Trigger** | `schedule`, `workflow_dispatch` |
| **Model** | (platform default) |
| **Permissions** | `contents: read`, `issues: read`, `pull-requests: read` |
| **Safe-Outputs** | `create-issue` (max 1, prefix `[simplisticate]`, labels: `agent:fixable`), `update-issue` (max 5), `add-comment` (max 1), `dispatch-workflow` (`sfl-issue-processor`, max 1) |
| **Input** | Full repository codebase |
| **Output** | Single consolidated `agent:fixable` issue |
| **Artifact State** | `010-issue-created` |

#### `daily-repo-status` — SFL Repo Status

| Field | Value |
|-------|-------|
| **Type** | Agentic (`.md` + `.lock.yml`) |
| **Schedule** | Daily |
| **Trigger** | `schedule: daily`, `workflow_dispatch` |
| **Model** | (platform default) |
| **Permissions** | `contents: read`, `issues: read`, `pull-requests: read`, `discussions: read` |
| **Safe-Outputs** | `create-discussion` (prefix `[repo-status]`), `update-discussion` (max 5), `add-comment` (max 1) |
| **Output** | Repo status Discussion |
| **Note** | Informational only — does not feed into the SFL fix pipeline |

---

### 3.2 Event-Driven Processing Workflows

#### `discussion-processor` — SFL Discussion Processor (Discussion → Issues)

| Field | Value |
|-------|-------|
| **Type** | Agentic (`.md` + `.lock.yml`) |
| **Trigger** | `discussion: [labeled]` (specifically the `action-item` label), `workflow_dispatch` |
| **Model** | (platform default) |
| **Permissions** | `contents: read`, `issues: read`, `pull-requests: read`, `discussions: read` |
| **Safe-Outputs** | `create-issue` (max 6, prefix `[auto]`, labels: `agent:fixable`, `action-item`), `update-discussion` (max 1), `add-labels` (max 1), `remove-labels` (max 1), `add-comment` (max 1) |
| **Input** | Discussion body containing structured findings |
| **Output** | Individual issues per finding group, discussion label flipped from `action-item` → `report` |
| **Artifact State** | Transitions Discussion from `010-issue-created` → issues at `020-issue-ready` |

#### `sfl-issue-categorizer` *(conceptual — handled by `discussion-processor`)*

> In the diagram this appears as a separate "SFL-Issue-Categorizer" box. In the actual implementation, the discussion-processor performs categorization inline — it reads findings, groups them, assigns risk labels, and creates appropriately labeled issues. The output fork to `020-issue-ready` (agent-fixable) vs `020-issue-ready` (human-required) happens within discussion-processor's logic.

---

### 3.3 Central Orchestrator

#### `sfl-dispatcher` — Lightweight Dispatch Gate

| Field | Value |
|-------|-------|
| **Type** | Standard YAML (no AI inference) |
| **Schedule** | `*/30 * * * *` (every 30 minutes) |
| **Trigger** | `schedule`, `workflow_dispatch` |
| **Permissions** | `contents: read`, `issues: read`, `pull-requests: read`, `actions: write`, `discussions: write` |
| **Concurrency** | Group: `sfl-dispatcher` (only one at a time) |
| **Purpose** | Polls GitHub state and dispatches agentic workflows only when work exists |

**Dispatch Logic:**

| Condition | Dispatches |
|-----------|-----------|
| Open issues with `agent:fixable` label exist | → `sfl-issue-processor` |
| Draft PRs with `agent:pr` + all 3 analyzer markers + not all PASS | → `pr-fixer` |
| Draft PRs with `agent:pr` + fixer completed previous cycle | → `sfl-analyzer-a`, `sfl-analyzer-b`, `sfl-analyzer-c` |
| Draft PRs with all analyzers PASS or non-draft PRs with `human:ready-for-review` | → `pr-promoter` |
| Draft PRs with `human:ready-for-review` label | → `pr-label-actions` (`SFL PR Label Actions`) |

**Pre-flight:** Runs a **model drift guard** that validates `sfl.json` model pins against `.lock.yml` files. Drift = hard failure.

---

### 3.4 Issue → PR Workflow

#### `sfl-issue-processor` — Claim Issue, Open Draft PR

| Field | Value |
|-------|-------|
| **Type** | Agentic (`.md` + `.lock.yml`) |
| **Trigger** | `workflow_dispatch` (dispatcher-only) |
| **Model** | (platform default) |
| **Timeout** | 60 minutes |
| **Permissions** | `contents: read`, `issues: read`, `pull-requests: read` |
| **Safe-Inputs** | `read-sfl-config` (reads `.github/sfl-config.yml`) |
| **Safe-Outputs** | `create-pull-request` (prefix `[agent-fix]`, labels: `agent:pr`, draft: true), `add-labels` (max 3), `remove-labels` (max 3), `update-issue` (max 3), `add-comment` (max 1) |
| **Input** | Oldest open `agent:fixable` issue |
| **Output** | Draft PR on branch `agent-fix/issue-N-*` |
| **Label Transitions** | Issue: `agent:fixable` → `agent:in-progress`; PR: gets `agent:pr` + `pr:cycle-1` |
| **Artifact State** | `020-issue-ready` → `030-pr-ready-for-review` |
| **Throughput** | Exactly 1 issue per run |

---

### 3.5 PR Analysis Workflows (Parallel)

All three analyzers run independently on the same PR. The value is **model diversity** — different AI models catch different issues.

#### `sfl-analyzer-a` — Full-Spectrum Review (Model A)

| Field | Value |
|-------|-------|
| **Type** | Agentic (`.md` + `.lock.yml`) |
| **Trigger** | `pull_request: [opened]`, `workflow_dispatch` (dispatcher for re-analysis) |
| **Model** | `claude-sonnet-4.6` |
| **Permissions** | `contents: read`, `issues: read`, `pull-requests: read` |
| **Safe-Outputs** | `update-discussion` (max 1), `update-issue` (max 2), `add-comment` (max 1) |
| **Idempotency** | Writes `[MARKER:sfl-analyzer-a cycle:N]` to PR body |
| **Verdict** | `**PASS**` or `**FAIL**` with structured review |
| **Throughput** | 1 PR per run |

#### `sfl-analyzer-b` — Full-Spectrum Review (Model B)

| Field | Value |
|-------|-------|
| **Type** | Agentic (`.md` + `.lock.yml`) |
| **Trigger** | `pull_request: [opened]`, `workflow_dispatch` (dispatcher for re-analysis) |
| **Model** | `claude-opus-4.5` |
| **Permissions** | `contents: read`, `issues: read`, `pull-requests: read` |
| **Safe-Outputs** | `update-discussion` (max 1), `update-issue` (max 2), `add-comment` (max 1) |
| **Idempotency** | Writes `[MARKER:sfl-analyzer-b cycle:N]` to PR body |
| **Verdict** | `**PASS**` or `**FAIL**` with structured review |
| **Throughput** | 1 PR per run |

#### `sfl-analyzer-c` — Full-Spectrum Review (Model C)

| Field | Value |
|-------|-------|
| **Type** | Agentic (`.md` + `.lock.yml`) |
| **Trigger** | `pull_request: [opened]`, `workflow_dispatch` (dispatcher for re-analysis) |
| **Model** | `gpt-5.3-codex` |
| **Permissions** | `contents: read`, `issues: read`, `pull-requests: read` |
| **Safe-Outputs** | `update-discussion` (max 1), `update-issue` (max 2), `add-comment` (max 1) |
| **Idempotency** | Writes `[MARKER:sfl-analyzer-c cycle:N]` to PR body |
| **Verdict** | `**PASS**` or `**FAIL**` with structured review |
| **Throughput** | 1 PR per run |

---

### 3.6 PR Fix & Promotion Workflows

#### `pr-fixer` — Implement Analyzer Fixes

| Field | Value |
|-------|-------|
| **Type** | Agentic (`.md` + `.lock.yml`) |
| **Trigger** | `workflow_dispatch` (dispatcher-only) |
| **Model** | `claude-opus-4.6` |
| **Permissions** | `contents: read`, `issues: read`, `pull-requests: read` |
| **Safe-Inputs** | `check-pr-merge-state` (GraphQL merge status), `resolve-pr-conflicts` (overwrite conflicting file with main) |
| **Safe-Outputs** | `push-to-pull-request-branch`, `add-labels` (max 3), `remove-labels` (max 3), `update-issue` (max 3), `add-comment` (max 1) |
| **Input** | Draft PR with all 3 analyzer markers at current cycle |
| **Output** | Fixes pushed to same PR branch, cycle label incremented |
| **Label Transitions** | `pr:cycle-N` → `pr:cycle-(N+1)` |
| **Idempotency** | Writes `[MARKER:pr-fixer cycle:N]` to PR body |
| **Max Cycles** | Configured in `sfl-config.yml` → `cycles.max-fix-cycles: 15` |
| **Throughput** | 1 PR per run |

#### `pr-promoter` — Un-draft & Merge

| Field | Value |
|-------|-------|
| **Type** | Agentic (`.md` + `.lock.yml`) |
| **Trigger** | `workflow_dispatch` |
| **Model** | `claude-sonnet-4.6` |
| **Permissions** | `contents: read`, `issues: read`, `pull-requests: read` |
| **Safe-Inputs** | None |
| **Safe-Outputs** | `add-labels` (max 3), `remove-labels` (max 3), `update-discussion` (max 2), `update-issue` (max 5), `add-comment` (max 1) |
| **Phase 1 — Promote** | All 3 analyzers PASS → adds `human:ready-for-review` + `agent:promoted` labels |
| **Artifact State** | `030-pr-ready-for-review` → `040-pr-ready-for-human-review` → **Done** |
| **Throughput** | 1 promotion per run |

#### `pr-label-actions` — SFL PR Label Actions

| Field | Value |
|-------|-------|
| **Type** | Standard YAML (no AI inference) |
| **Trigger** | `pull_request: [labeled]`, `workflow_dispatch` (dispatcher) |
| **Permissions** | `pull-requests: write` |
| **Operations** | `human:ready-for-review` on draft → flips to non-draft |

---

### 3.7 Health & Consistency

#### `sfl-auditor` — Label Consistency Auditor

| Field | Value |
|-------|-------|
| **Type** | Agentic (`.md` + `.lock.yml`) |
| **Schedule** | `15 * * * *` (hourly, offset 15 min) |
| **Trigger** | `schedule`, `workflow_dispatch` |
| **Model** | `claude-sonnet-4.6` |
| **Permissions** | `contents: read`, `issues: read`, `pull-requests: read` |
| **Safe-Outputs** | `update-discussion` (max 1), `update-issue` (max 10), `add-comment` (max 1) |
| **Purpose** | Audits issue↔PR label relationships and repairs discrepancies |
| **Dashboard** | Updates Discussion #51 section `<!-- SECTION:sfl-auditor -->` |

---

## 4. Sequence — Step by Step

```
PHASE 1 — DISCOVERY (Daily)
════════════════════════════
  ┌─────────────────┐    ┌─────────────────────┐
  │   repo-audit    │    │   simplisticate     │    ← Scheduled daily
  │  (24h cron)     │    │  (24h cron, 1AM)    │
  └────────┬────────┘    └──────────┬──────────┘
           │                        │
           ▼                        ▼
    Discussion with            Issues with
    [repo-audit] prefix        [simplisticate] prefix
       Labels: (none yet)         Labels: agent:fixable
           │                        │
           │   ┌────────────────────┘
           │   │  Human or workflow labels
           ▼   ▼  discussion with `action-item`
    ┌─────────────────┐
    │  ARTIFACT STATE │
    │ 010-issue-created│    ← Discussion or Issue exists with findings
    └────────┬────────┘

PHASE 2 — CATEGORIZATION (Event-driven)
════════════════════════════════════════
           │
           ▼
    ┌─────────────────────┐
       │ SFL Discussion      │    ← Triggered by `action-item` label on discussion
       │ Processor           │
    │  (event: labeled)    │
    └────────┬─────────────┘
             │ Groups findings → creates issues
             │ Flips discussion label: action-item → report
             │
             ├──────────────────────┐
             ▼                      ▼
      ┌─────────────┐       ┌──────────────┐
      │ ARTIFACT     │       │ ARTIFACT      │
      │020-issue-    │       │020-issue-     │
      │ ready        │       │ ready         │
      │(agent:fixable│       │(agent:human-  │
      │+ action-item)│       │ required)     │
      └──────┬───────┘       └──────────────┘
             │                   (exits pipeline — human owns)
             │

PHASE 3 — DISPATCH (Every 30 min)
══════════════════════════════════
             │
             ▼
    ┌──────────────────┐
    │  sfl-dispatcher   │    ← Standard YAML, no AI inference
    │  (*/30 cron)      │       Polls GitHub state, dispatches work
    │                   │
    │  Model drift      │    ← Pre-flight: validates sfl.json vs .lock.yml
    │  guard runs first │
    └────────┬──────────┘
             │  Dispatches sfl-issue-processor when agent:fixable issues exist
             ▼

PHASE 4 — ISSUE → PR (Dispatcher-triggered)
════════════════════════════════════════════
    ┌──────────────────┐
       │ sfl-issue-processor│    ← Claims oldest agent:fixable issue
    │ (workflow_dispatch)│      Implements fix on new branch
    └────────┬──────────┘      Opens draft PR with [agent-fix] prefix
             │
             │  Issue: agent:fixable → agent:in-progress
             │  PR: labeled agent:pr, draft: true
             ▼
      ┌─────────────────┐
      │  ARTIFACT STATE  │
      │ 030-pr-ready-   │    ← Draft PR exists, awaiting analyzer reviews
      │  for-review      │
      └────────┬─────────┘

PHASE 5 — PARALLEL ANALYSIS (Event-driven + Dispatcher)
════════════════════════════════════════════════════════
             │
             │  Triggered by pull_request:opened OR dispatcher (re-analysis)
             │
    ┌────────┼────────────────┐
    │        │                │
    ▼        ▼                ▼
┌────────┐ ┌────────┐ ┌────────┐
│Analyzer│ │Analyzer│ │Analyzer│    ← 3 independent full-spectrum reviews
│   A    │ │   B    │ │   C    │       Different AI models for diversity
│Sonnet  │ │ Opus   │ │Codex   │
│ 4.6    │ │  4.5   │ │ 5.3    │
└───┬────┘ └───┬────┘ └───┬────┘
    │          │          │
    │  Each writes [MARKER:pr-analyzer-X cycle:N] + verdict (PASS/FAIL)
    │          │          │
    └──────────┼──────────┘
               │
               ▼
        All 3 markers present at cycle N?
               │
          ┌────┴────┐
          │         │
     All PASS    Any FAIL
          │         │
          ▼         ▼

PHASE 6a — FIX CYCLE (Dispatcher-triggered, if any FAIL)
═════════════════════════════════════════════════════════
               │
               ▼
    ┌──────────────────┐
    │    pr-fixer       │    ← Reads all analyzer comments
    │(workflow_dispatch)│       Implements fixes
       │ Model: Opus 4.6  │       Pushes to same PR branch
    └────────┬─────────┘       Increments pr:cycle-N label
             │
             │  Writes [MARKER:pr-fixer cycle:N]
             │  Label: pr:cycle-N → pr:cycle-(N+1)
             ▼
    ┌──────────────────┐
    │  LOOP BACK TO    │    ← Dispatcher detects fixer completed
    │  PHASE 5         │       Dispatches all 3 analyzers for re-analysis
    │  (re-analysis)   │       Max cycles: 15 (from sfl-config.yml)
    └──────────────────┘

PHASE 6b — PROMOTION (Dispatcher-triggered, if all PASS)
════════════════════════════════════════════════════════
               │
               ▼
    ┌──────────────────┐
    │   pr-promoter     │    ← Phase 1: Un-drafts the PR
    │(workflow_dispatch)│       Adds human:ready-for-review + agent:promoted
    │ Model: Sonnet 4.6│
    └────────┬─────────┘
             │
             ▼
      ┌─────────────────┐
      │  ARTIFACT STATE  │
      │ 040-pr-ready-   │    ← PR is non-draft, labeled for human review
      │  for-human-review│
      └────────┬─────────┘

PHASE 7 — HUMAN MERGE DECISION
═══════════════════════════════
             │
             ▼
      ┌───────────────┐
      │ Human Review  │
      │ + Merge       │
      └───────────────┘
   │ delete    │
   │ branch +  │
   │ close     │
   │ issue     │
   └──────────┘

CONTINUOUS — HEALTH CHECK (Hourly)
══════════════════════════════════
    ┌──────────────────┐
    │   sfl-auditor     │    ← Runs every hour (offset :15)
    │  (15 * * * *)     │       Detects label↔PR inconsistencies
    │ Model: Sonnet 4.6 │       Repairs state discrepancies
    └──────────────────┘       Updates Dashboard Discussion #51
```

---

## 5. State Machine Summary

### Artifact States (as shown in diagram)

| State | Description | GitHub Primitive | Transitions To |
|-------|-------------|-----------------|---------------|
| `010-issue-created` | Finding detected, Discussion/Issue exists | Discussion or Issue with `action-item` | `020-issue-ready` (via discussion-processor) |
| `020-issue-ready` | Issue triaged with `agent:fixable` or `agent:human-required` | Issue with lifecycle label | `030-pr-ready-for-review` (via sfl-issue-processor) |
| `030-pr-ready-for-review` | Draft PR opened, awaiting analyzer reviews + fix cycles | Draft PR with `agent:pr` label | `040-pr-ready-for-human-review` (via pr-promoter) |
| `040-pr-ready-for-human-review` | PR un-drafted, all analyzers PASS, awaiting human | Non-draft PR with `human:ready-for-review` | **Merged** (human action) |

### Issue Label State Machine

```
agent:fixable  →  agent:in-progress  →  (PR merged → issue closed)
                                     →  agent:human-required (if risk too high)
                                     →  agent:escalated (if repeated failures)
                                     →  agent:pause (if blocked)
```

### PR Label State Machine

```
agent:pr + pr:cycle-1
       → [analyzers review] → pr:cycle-1 markers written
  → [fixer pushes fixes] → pr:cycle-1
  → [analyzers re-review] → pr:cycle-1 markers written
  → … (repeat up to max-fix-cycles)
       → [all PASS] → agent:promoted + human:ready-for-review
       → [human review + merge] → MERGED
```

### Configuration

| Setting | Value | Source |
|---------|-------|--------|
| Auto-merge | `true` | `.github/sfl-config.yml` |
| Auto-create PR | `true` | `.github/sfl-config.yml` |
| Conflict resolution | `true` | `.github/sfl-config.yml` |
| Risk tolerance | `low` | `.github/sfl-config.yml` |
| Max fix cycles | `15` | `.github/sfl-config.yml` |
| Activity log | Discussion #95 | `.github/sfl-config.yml` |
| Status dashboard | Discussion #51 | Workflow prompts |

### Model Assignments

| Workflow | Model | Pinned In |
|----------|-------|-----------|
| sfl-analyzer-a | `claude-sonnet-4.6` | `sfl.json` |
| sfl-analyzer-b | `claude-opus-4.5` | `sfl.json` |
| sfl-analyzer-c | `gpt-5.3-codex` | `sfl.json` |
| pr-fixer | `claude-opus-4.6` | Workflow frontmatter |
| pr-promoter | `claude-sonnet-4.6` | Workflow frontmatter |
| sfl-auditor | `claude-sonnet-4.6` | Workflow frontmatter |
