# Debug history, 2026-08-27

## 13:01 - Diagnosed stale HemSoft Copilot quota and created issue #594

Confirmed the live v0.1.1049 account snapshot had no provider or saved override for `hemsoft/hemsoft`, causing the namespace overview to fall through to Copilot despite the existing Codex weekly-usage client, and filed follow-up issue #594. Retrospective found no reusable debug skill change because the existing read-only CDP and persisted-state workflow isolated the provider-assignment gap without restarting the app.

## 16:28 - Verified the Issue #594 fix in isolated Electron

Started the issue worktree's Electron build with isolated user data and inspected it over CDP. The HemSoft overview rendered `Codex Allowance`, the weekly allowance metrics, and the ChatGPT/Codex Pro plan while omitting `Copilot Quota`, confirming the provider switch without disturbing the already-running primary app. Retrospective found no reusable debug skill change because the isolated-CDP workflow already supplied the required runtime proof.
